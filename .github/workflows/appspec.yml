on:
  push:
    branches:
      - release/v0.1.0

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Deploy application
        run: scripts/gh_deploy.sh
